{{- if .Values.hooks.enable }}
apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    {{- include "cdi.labels" . | nindent 4 }}
    {{- with .Values.hooks.jobLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  name: {{ printf "%s-namespace" (include "cdi.fullname" .) }}
  namespace: {{ .Release.Namespace }}
spec:
  template:
    metadata:
      {{- with .Values.hooks.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
      {{- with .Values.hooks.podLabels }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
        {{- include "cdi.selectorLabels" . | nindent 8 }}
      name: {{ printf "%s-namespace" (include "cdi.fullname" .) }}
    spec:
      {{- with .Values.hooks.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Never
      serviceAccountName: {{ include "cdi.hook.serviceaccount" . }}
      {{- with .Values.hooks.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: label-namespace
        image: "{{ default .Values.hooks.image.registry .Values.global.imageRegistry }}/{{ .Values.hooks.image.repository }}:{{ .Values.hooks.image.tag }}"
        imagePullPolicy: {{ .Values.hooks.image.pullPolicy }}
        command:
        - /bin/bash
        - -c
        - |
          set -ex
          kubectl label namespace {{ .Release.Namespace }} cdi.kubevirt.io= --overwrite
        {{- with .Values.hooks.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
{{- end }}
