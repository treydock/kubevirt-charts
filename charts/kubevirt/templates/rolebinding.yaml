apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    kubevirt.io: ""
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ printf "%s-rolebinding" (include "kubevirt.name" .) }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kubevirt.name" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "kubevirt.name" . }}
  namespace: {{ .Release.Namespace }}
