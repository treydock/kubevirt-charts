{{- if and .Values.hooks.enable }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ printf "%s-namespace" (include "kubevirt.fullname" .) }}
rules:
  - apiGroups:
      - ""
    resources:
      - namespaces
    resourceNames:
      - {{ .Release.Namespace }}
    verbs:
      - get
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ printf "%s-namespace" (include "kubevirt.fullname" .) }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ printf "%s-namespace" (include "kubevirt.fullname" .) }}
subjects:
- kind: ServiceAccount
  name: {{ printf "%s-hook" (include "kubevirt.fullname" .) }}
  namespace: {{ .Release.Namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  annotations:
    "helm.sh/hook": post-install,post-upgrade,pre-delete
    "helm.sh/hook-weight": "2"
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ printf "%s-hook" (include "kubevirt.fullname" .) }}
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups:
      - ""
    resources:
      - namespaces
    verbs:
      - get
      - patch
  - apiGroups:
      - kubevirt.io
    resources:
      - kubevirts
    verbs:
      - get
      - list
      - watch
      - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  annotations:
    "helm.sh/hook": post-install,post-upgrade,pre-delete
    "helm.sh/hook-weight": "2"
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ printf "%s-hook" (include "kubevirt.fullname" .) }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ printf "%s-wait" (include "kubevirt.fullname" .) }}
subjects:
- kind: ServiceAccount
  name: {{ printf "%s-hook" (include "kubevirt.fullname" .) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
