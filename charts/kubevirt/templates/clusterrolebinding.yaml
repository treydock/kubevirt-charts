apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    kubevirt.io: ""
    {{- include "kubevirt.labels" . | nindent 4 }}
  name: {{ include "kubevirt.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubevirt.name" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "kubevirt.name" . }}
  namespace: {{ .Release.Namespace }}
